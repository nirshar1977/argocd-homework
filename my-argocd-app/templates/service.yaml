apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-argocd-app.fullname" . }}
  labels:
    {{- include "my-argocd-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-argocd-app.selectorLabels" . | nindent 4 }}
